# module-tsl-environment

Detects the runtime environment as follows

1. Use TSL_ENV if set
2. Use NODE_ENV if set
3. development

### Example Usage
```js
var environment = require('module-tsl-environment');
``` 
